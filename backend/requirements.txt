fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# CPU-only torch wheels — dramatically smaller than the default CUDA build.
# ~250 MB download vs ~800 MB for the default. Render free tier has no GPU anyway.
# Install with: pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2

Pillow>=10.0
numpy==1.26.4

pydantic==2.11.0
pydantic-settings>=2.5.0